<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage -->
    <external-path
        name="external_files"
        path="." />

    <!-- External files directory -->
    <external-files-path
        name="external_files_path"
        path="." />

    <!-- Cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- External cache directory -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Files directory -->
    <files-path
        name="files"
        path="." />

    <!-- For camera photos -->
    <external-files-path
        name="my_images"
        path="Pictures/" />

    <external-files-path
        name="my_camera_images"
        path="DCIM/" />
</paths>